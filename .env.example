# SignalOps environment variables
#
# Copy to .env.local and fill in values for LIVE investigation runs.
# Not required for install, build, tests, or replay-mode UI — the committed
# fixtures, embeddings and run artefacts drive everything else.

# Anthropic API key — required only for live investigation/critic model calls.
ANTHROPIC_API_KEY=
